<div class="container py-5">

    <a href="/events" class="btn btn-outline-primary mb-4">
        <i class="bi bi-arrow-left"></i> Voltar para Eventos
    </a>

    <div class="card shadow-sm mb-4">
        <div class="card-header bg-white fw-bold text-primary d-flex justify-content-between align-items-center">
            <div>
                <i class="bi bi-calendar-event me-2"></i> <?= $event->name ?>
            </div>
            <div class="d-flex align-items-center">
                <a href="<?= route('events.edit', ['event_id' => $event->id]) ?>" class="me-3">
                    <i class="bi bi-pencil-square text-secondary-custom fs-5"></i>
                </a>
                <button type="button" class="btn btn-link p-0 m-0" data-bs-toggle="modal" data-bs-target="#deleteModal">
                    <i class="bi bi-trash3 text-danger-custom fs-5"></i>
                </button>
            </div>
        </div>
        <div class="card-body">
            <p class="text-muted">Detalhes do evento estão listados abaixo.</p>
            <ul class="list-unstyled">
                <li><strong>Descrição:</strong> <?= $event->description ?></li>
                <li><strong>Categoria:</strong> <?= $event->category ?></li>
                <li><strong>Início:</strong> <?= $event->start_date ?></li>
                <li><strong>Fim:</strong> <?= $event->finish_date ?></li>
                <li><strong>Local:</strong> <?= $event->location_name ?> - <?= $event->address ?></li>
            </ul>
        </div>
    </div>

    <div class="modal fade" id="deleteModal" tabindex="-1" aria-labelledby="deleteModalLabel" aria-hidden="true">
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title fw-bold text-danger" id="deleteModalLabel">
                        <i class="bi bi-exclamation-triangle-fill me-2"></i>Confirmar Exclusão
                    </h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Fechar"></button>
                </div>
                <div class="modal-body">
                    <p>Você tem certeza de que deseja excluir este evento?</p>
                    <p class="text-danger-custom">Esta ação não pode ser desfeita!</p>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancelar</button>
                    <form action="<?= route('events.destroy', ['event_id' => $event->id]) ?>" method="POST">
                        <input type="hidden" name="_method" value="DELETE">
                        <button type="submit" class="btn btn-danger">Excluir Evento</button>
                    </form>
                </div>
            </div>
        </div>
    </div>

</div>
