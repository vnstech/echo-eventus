<div class="container py-5">
    <a href="/events" class="btn btn-outline-primary mb-4">
        <i class="bi bi-arrow-left"></i> Back to Events
    </a>

    <div class="text-center mb-5">
        <h1 class="fw-bold text-primary">Edit Event</h1>
        <p class="text-muted">Update the event name below</p>
    </div>

    <form action="/events/<?= $event->id ?>" method="POST">
        <input type="hidden" name="_method" value="PUT">

        <input type="hidden" name="event" value="<?= $event->id ?>">

        <div class="card shadow-sm mb-4">
            <div class="card-header bg-white fw-bold text-primary">
                <i class="bi bi-pencil-square me-2"></i> Edit Event Name
            </div>
            <div class="card-body">
                <div class="mb-3">
                    <label for="eventName" class="form-label">Event Name</label>
                    <input type="text" class="form-control" id="eventName" name="name" required value="<?= htmlspecialchars($event->name) ?>">
                </div>
            </div>
        </div>

        <div class="d-flex justify-content-center gap-2">
            <button type="submit" class="btn btn-primary">Update Event</button>
        </div>
    </form>
</div>
