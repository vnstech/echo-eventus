<div class="container-fluid px-4">
    <div class="row g-4">
        
        <h1 class="fw-bold text-primary">Events</h1>
        <?php foreach($events as $event): ?>
            <div class="col-12 col-sm-6 col-md-4 col-lg-3">
                <div class="card h-100 shadow-sm border-0 rounded-4 hover-shadow transition">
                    <div class="card-body d-flex flex-column p-4">
                        <h5 class="card-title mb-2">
                            <a href="<?= route('events.show', ['event_id' => $event->id]) ?>"
                                class="text-decoration-none text-dark fw-semibold">
                                <?= htmlspecialchars($event->name) ?>
                            </a>
                        </h5>

                        <p class="card-text text-muted small mb-3">
                            <?= htmlspecialchars($event->description) ?>
                        </p>

                        <div class="mt-auto">
                            <h6 class="fw-bold text-secondary small">Date and Location</h6>
                            <p class="mb-1 text-dark">
                                <i class="bi bi-geo-alt-fill text-primary me-1"></i>
                                <?= htmlspecialchars($event->location_name) ?>
                            </p>
                            <p class="mb-1 text-dark small">
                                <strong>Start:</strong> <?= htmlspecialchars($event->start_date) ?>
                            </p>
                            <p class="mb-3 text-dark small">
                                <strong>End:</strong> <?= htmlspecialchars($event->finish_date) ?>
                            </p>

                            <div class="d-flex justify-content-between align-items-center mt-2">
                                <span class="badge bg-light text-secondary rounded-pill px-3 py-2 shadow-sm text-capitalize">
                                    <?= htmlspecialchars($event->status) ?>
                                </span>

                                <a href="<?= route('events.show', ['event_id' => $event->id]) ?>"
                                    class="btn btn-sm btn-outline-primary d-flex align-items-center gap-1">
                                    <i class="bi bi-gear"></i> Manage
                                </a>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        <?php endforeach; ?>
    </div>
    <?php if (!empty($events)): ?>
        <div class="d-flex justify-content-center mt-4">
            <?php $paginator->renderPagesNavigation(); ?>
        </div>
    <?php endif; ?>
</div>