<div class="row g-4">
    <?php foreach($events as $event): ?>
        <div class="col-12 col-sm-6 col-md-4 col-lg-3">
            <div class="card h-100 shadow-sm">
                <div class="card-body d-flex flex-column">
                    <h5 class="card-title mb-2">
                        <a href="<?= route('events.show', ['event_id' => $event->id]) ?>" class="text-decoration-none text-dark">
                            <?= htmlspecialchars($event->name) ?>
                        </a>
                    </h5>
                    <p class="card-text text-muted mb-3"><?= htmlspecialchars($event->description) ?></p>

                    <div class="mt-auto">
                        <h6 class="fw-bold">Date and Location</h6>
                        <p class="mb-1"><i class="bi bi-geo-alt"></i> <?= htmlspecialchars($event->location_name) ?></p>
                        <p class="mb-1"><strong>Start:</strong> <?= htmlspecialchars($event->start_date) ?></p>
                        <p class="mb-3"><strong>End:</strong> <?= htmlspecialchars($event->finish_date) ?></p>

                        <div class="d-flex justify-content-between align-items-center">
                            <span class="badge bg-light text-dark px-3 py-2">
                                <?=htmlspecialchars($event->status) ?>
                            </span>

                            <a href="<?= route('events.show', ['event_id' => $event->id]) ?>" class="btn btn-sm btn-primary">
                                Manage
                            </a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    <?php endforeach; ?>
</div>

<?php $paginator->renderPagesNavigation(); ?>
